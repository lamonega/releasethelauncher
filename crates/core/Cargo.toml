[package]
name = "release-the-launcher-core"
version = { workspace = true }
edition = "2021"
description = "Instance model, filesystem, settings, archive extraction"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "1"
zip = "8.6.0"
thiserror = "2"
sha1 = "0.10"
sha2 = "0.10"
hex = "0.4"
log = "0.4"
release-the-launcher-constants = { path = "../constants" }
