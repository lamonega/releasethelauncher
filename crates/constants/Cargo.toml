[package]
name = "release-the-launcher-constants"
version = { workspace = true }
edition = "2021"
description = "Centralized constants for Release The Launcher"
license.workspace = true

[dependencies]
